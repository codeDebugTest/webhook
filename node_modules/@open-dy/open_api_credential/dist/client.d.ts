import { TokenResponse } from './type';
export declare class Config {
    clientKey: string;
    clientSecret: string;
    constructor(map?: {
        [key: string]: any;
    });
}
export default class Client {
    private clientKey;
    private clientSecret;
    private clientToken;
    private clientTokenExpires;
    private accessToken;
    private accessTokenExpires;
    constructor(config: Config);
    getClientKey(): string;
    getClientSecret(): string;
    private formatErrorMessage;
    private getClientTokenHttp;
    setClientTokenExpires(expires: number): void;
    getClientToken(): Promise<TokenResponse>;
    private getAccessTokenHttp;
    setAccessTokenExpires(expires: number): void;
    getAccessToken(): Promise<TokenResponse>;
}
